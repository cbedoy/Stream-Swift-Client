# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Stream Client' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  #use_frameworks!

  # Pods for Stream Client
  pod 'GetStream', '~> 1.0'
  
  target 'Stream ClientTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Stream ClientUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
